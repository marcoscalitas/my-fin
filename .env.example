# Vercel Postgres (gerado automaticamente pelo Vercel ao conectar o Neon)
POSTGRES_URL=
POSTGRES_HOST=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Sessão — string aleatória com 32+ caracteres
SESSION_SECRET=

# Token para criar tabelas via POST /api/setup (usar uma única vez)
SETUP_TOKEN=
